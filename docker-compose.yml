version: "3.9"
services:
  web:
    build: .
    depends_on:
      - redis
    ports:
      - "8080:8080"
  redis:
    hostname: redis
    image: redis
    restart: always
    ports:
      - "6379:6379"